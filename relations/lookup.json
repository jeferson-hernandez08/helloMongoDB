db.cliente.aggregate([
    {
        $lookup: {
            from: "producto",
            localField: "producto.productoId",
            foreignField: "_id",
            as: "detalleCompra"
        }
    }
])

